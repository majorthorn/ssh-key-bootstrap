# Example configuration for ssh-key-bootstrap
# Uses env var for password to avoid plaintext secrets in this file.

SERVERS=app01.internal,app02.internal:2222
USER=deploy
# Set one of PASSWORD or PASSWORD_SECRET_REF (not both).
PASSWORD=replace-with-your-password
PASSWORD_SECRET_REF=bw://replace-with-your-secret-id
# PASSWORD_SECRET_REF=infisical://replace-with-your-secret-name
KEY=~/.ssh/id_ed25519.pub
PORT=22
TIMEOUT=10
KNOWN_HOSTS=~/.ssh/known_hosts
INSECURE_IGNORE_HOST_KEY=false

# Infisical provider (SDK + Universal Auth)
# Required when PASSWORD_SECRET_REF uses infisical:// or inf://

# Infisical host (required for self-host, optional for cloud)
# Must be HTTPS host only; no path/query. Default: https://app.infisical.com
# INFISICAL_SITE_URL=https://app.infisical.com

# Universal Auth credentials (required)
# INFISICAL_UNIVERSAL_AUTH_CLIENT_ID=replace-with-your-client-id
# INFISICAL_UNIVERSAL_AUTH_CLIENT_SECRET=replace-with-your-client-secret

# Optional organization slug for scoped machine identity auth
# INFISICAL_AUTH_ORGANIZATION_SLUG=replace-with-your-org-slug

# Project/environment target (required)
# INFISICAL_PROJECT_ID=replace-with-your-project-id
# INFISICAL_ENV=prod
# INFISICAL_ENVIRONMENT=prod
